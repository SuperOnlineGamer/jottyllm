# For more advanced settings and more information about the env variables,
# please read https://github.com/fccview/jotty/blob/main/howto/DOCKER.md

services:
  jottyllm:
    image: jottyllm:latest
    container_name: jottyllm
    user: "1000:1000"
    ports:
      - "1122:3000"
    volumes:
      - ./data:/app/data:rw
      - ./config:/app/config:rw
      - ./cache:/app/.next/cache:rw
      #- ./user_patches:/app/user_patches:ro
    restart: unless-stopped
    #platform: linux/arm64
    environment:
      - NODE_ENV=production
      # AI editor - provide your OpenAI API key here or via OPENAI_API_KEY_FILE
      #- OPENAI_API_KEY=sk-...
      #- OPENAI_API_KEY_FILE=/app/config/openai-api-key.txt
      # Ollama is configured via the Admin panel (no env var needed)
      #- HTTPS=true
      #- APP_URL=https://your-jotty-domain.com
      #- INTERNAL_API_URL=http://localhost:3000
      #- SERVE_PUBLIC_IMAGES=yes
      #- SERVE_PUBLIC_FILES=yes
      #- AUTH_MODE=oidc
      #- OIDC_ISSUER=<YOUR_SSO_ISSUER>
      #- OIDC_CLIENT_ID=<YOUR_SSO_CLIENT_ID>
      #- OIDC_CLIENT_SECRET=your_client_secret
      #- SSO_FALLBACK_LOCAL=yes
      #- OIDC_ADMIN_GROUPS=admins
      #- ENABLE_API_DOCS=true
      #- JOTTY_BODY_SIZE_LIMIT=100mb
